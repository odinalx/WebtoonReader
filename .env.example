# Copy to .env and adjust. Values are baked into the build (import.meta.env).
#
# Origin of the Sori website — the extension calls its /api/* endpoints and
# links to it for sign-up / pricing. Local for now; switch to the production
# domain when the site is deployed, then rebuild the extension.
WXT_SITE_URL=http://localhost:3000
